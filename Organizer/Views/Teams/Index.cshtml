@model ParentViewModel
@{
    ViewData["title"] = "Index";
}

<h1>Index</h1>


<p>
    <a asp-action="Details">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                email
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <form method="post" action="@Url.Action("LeaveTeam", "Teams", new { user_id = "ErI23EPbSbsniBExNcOfLBoQt_wb0j__PPz320hXzog", team_id = "e6f8c7d0-1a34-4872-bb95-b4d8e0c1f2c9" })">
            <button type="submit">Leave Team</button>
        </form>
        @foreach (var item in Model.Users)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.id)
                </td>
                <br />
                <td>

                    @Html.DisplayFor(modelItem => item.tenant_id)
                </td>

                <td>
                    <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
    <tbody>

        @foreach (var item in Model.Teams)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.title)
                </td>
                <td>

                    @Html.DisplayFor(modelItem => item.id)
                </td>

                <td>
                    <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
